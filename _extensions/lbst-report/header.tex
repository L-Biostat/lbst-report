% load packages
\usepackage{geometry}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{pdflscape}
\usepackage[T1]{fontenc}
\usepackage{helvet}
\usepackage{titling}

% Use Helvetica as the default font
\renewcommand{\familydefault}{\sfdefault}

% Define landscape commands
\newcommand{\blandscape}{\begin{landscape}}
    \newcommand{\elandscape}{\end{landscape}}

% Define some colours
\definecolor{lightgray}{gray}{0.5}

% Define commands for the title and the date in the header
\makeatletter
\newcommand{\headerdate}{\@date}
\newcommand{\headertitle}{\MakeUppercase{\@title}}
\makeatother

% Define the style of all other pages 'basestyle'
\fancypagestyle{basestyle}{
  \fancyhf{}
  \fancyhead[C]{\textcolor{lightgray}{\footnotesize \sffamily \headertitle}}
  \fancyfoot[L]{\textcolor{lightgray}{\footnotesize \sffamily \headerdate}}
  \fancyfoot[R]{\textcolor{lightgray}{\sffamily \thepage/\pageref*{LastPage}}}
  % Remove the line below the header
  \renewcommand{\headrulewidth}{0pt}
}

% Apply the base style in general
\pagestyle{basestyle}

% No header or footer on the titlepage
\thispagestyle{empty}

% Redefine the title page
\pretitle{\begin{center}\Huge\textbf}
\posttitle{\par \lineskip 0.5em \huge Statistical report
\par \end{center}\vskip 0.5em}
\preauthor{\begin{center}
\Large \lineskip 0.5em%
\begin{tabular}[t]{c}}
\postauthor{\end{tabular}\par\end{center}}
\predate{\begin{center}\Large}
\postdate{\par\end{center}}